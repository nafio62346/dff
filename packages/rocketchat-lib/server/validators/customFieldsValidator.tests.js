describe('Validators', function () {
	describe('customFieldsValidator', function () {
		it('should return an error if the Accoount_CustomFields setting is not present', function () {
			fail('Not implemented');
		});
		it('should throw an error if the Accounts_CustomFields is invalid JSON', function () {
			fail('Not implemented');
		});

		it('should throw an error if a required field is not present', function () {
			// return a custom RocketChat.settings.get('Accounts_CustomFields') for testing
			fail('Not implemented');
		});

		// other test cases
		// not valid
		// max length
		// min length
	});
});
